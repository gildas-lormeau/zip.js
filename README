zip.js is a library for zipping and unzipping files.

It offers a low-level API for writing and reading (big) zip files and supports UTF8 or ASCII filenames.

This library is implemented with these HTML5 features: 
- Web workers
- Typed arrays
- Blob and BlobBuilder
- FileReader
- FileWriter

HOWTO:
- include zip.js in your project

- write a zip file:
var zipper = zip.createWriter(/* File */file);
- add a file into the zip:
zipper.add(/* String */filename, /* Array or Uint8Array */data, /* options */null, function() {
	// end callback 
}, function(/* Number */current, /* Number */total) {
	// zip progress callback
});
- close the zip file
zipper.close(function() {
    // onclose callback
});

- read a zip file:
var unzipper = zip.createReader(/* File */file);
- list all entries from the zip:
unzipper.getEntries(function(/* Array */entries) {
	// entries is an array of Object with zip entry details
});
- get the content from a zip entry:
entry.getData(function(/* Array or Uint8Array */ data) {
	// data contains the file entry content
}, function(/* Number */current, /* Number */total) {
	// unzip progress callback
});
- close the zip file
unzipper.close(function() {
    // onclose callback
});
